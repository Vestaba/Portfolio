# Dataset Description

This project uses survey data originally stored in SPSS `.sav` format.

The dataset was converted to `.csv` using a Python script (`scripts/convert_sav_to_csv.py`) to make it easier to process in data analysis tools such as SQL Server and Python.

## Files

- datasav.sav – original dataset in SPSS format
- datasav.csv – converted dataset used for analysis
